//Configure Git (one time only)

git config --global user.name "Your Name"
git config --global user.email "your_email@example.com"

//Create a new repository

touch README.md
git init
git add README.md
git commit -m "first commit"
git remote add origin https://github.com/<your-user-name>/<project-name>.git
git push -u origin master

git add README.MD
git commit
git commit -m "My initial commit"
git commit -a
git log

//Use an existing repository

git clone https://github.com/WordPress/WordPress
//Clone local repository (?)
git clone --no-hardlinks /path/to/repo 
git pull
git push

https://github.com/yuzhangecho/WorkDocs.git

//Reset all the changes from the last commit

git diff
git reset --hard
git diff
git status

//Fork egs: Push/Pull 

git push origin master
git fetch upstream
git merge upstream/master

//Branches

git branch mybranch
git checkout mybranch
git checkout -b mybranch
git checkout master
git checkout mybranch
git checkout master
git merge mybranch
git branch -d mybranch
#delete the "mybranch" branch

Webpage based triggers: GitHub
Client Git GUI - ver(0.18.GITGUI)

///////////////////////////////////////////////////////////////////////////////
//Info webpage based 
1. After select a project, when "Clone in Desktop", chrome triggers external app.
2. In option, change default storage directory dir.
3. Can manually delete the previous dir and after change, can update to new dir. (step unclear)
4. Public vs private

////Links:
https://help.github.com/articles/using-pull-requests
